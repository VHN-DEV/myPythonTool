# Requirements cho myPythonTool
# Cài đặt: pip install -r requirements.txt

# Tool compress-images
Pillow>=10.0.0

# Tool text-encoding-converter
chardet>=5.0.0

# Tool qr-code (tạo và giải mã QR code)
qrcode>=7.4.2        # Tạo QR code (cần Pillow, đã có ở trên)
opencv-python>=4.8.0  # Giải mã QR code
pyzbar>=0.1.9        # Giải mã QR code và barcode
numpy>=1.24.0        # Xử lý ảnh (cho opencv)

# Tool pdf-tools
PyPDF2>=3.0.0
pdf2image>=1.16.0    # Cần cài thêm poppler-utils (xem hướng dẫn trong README)

# Tool video-converter
moviepy>=1.0.3       # Cần cài thêm ffmpeg (xem hướng dẫn trong README)

# Tool extract-archive (tùy chọn)
# Bỏ comment nếu cần hỗ trợ thêm định dạng nén
# py7zr>=0.20.0        # Hỗ trợ .7z
# rarfile>=4.0         # Hỗ trợ .rar (cần cài WinRAR/unrar)

# UI/UX improvements
colorama>=0.4.6        # Cross-platform colored terminal output

